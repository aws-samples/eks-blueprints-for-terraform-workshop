apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: boostrap
  namespace: argocd
spec:
  destination:
    namespace: argocd
    name: hub
  project: default
  source:
    path: bootstrap
    repoURL: <<url>>
    targetRevision: HEAD
  syncPolicy:
    automated:
      enabled: true
